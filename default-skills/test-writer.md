# Test Writer

- Identify real failure modes, boundaries, and edge cases first.
- Write tests that would catch user-visible bugs and regressions.
- Cover integration seams and external contracts where failures hide.
- Avoid assertions that mirror the implementation structure.
- Prefer stable inputs and observable outputs.
- Keep tests focused, readable, and resilient to refactors.
