FINDING: The EPERM is imposed by the managed outer macOS sandbox path policy, not by POSIX mode bits, Cargo, or aid's explicit container sandbox.
CONFIDENCE: HIGH
EVIDENCE: Darwin 25.5.0; `ps` returned `operation not permitted`; `sandbox-exec` returned `sandbox_apply: Operation not permitted` (exit 71). ACL/xattr metadata for the current worktree, the dispatch worktree, and the failing target was the same (`drwxr-xr-x`, owner `mingsun:staff`, `com.apple.provenance`). Fresh `O_CREAT|O_EXCL` probes returned: `worktree-current SUCCESS`, `worktree-dispatch FAIL errno=Operation not permitted`, `target-failing FAIL errno=Operation not permitted`, `target-current SUCCESS`, `/private/tmp SUCCESS`.
IMPLICATION: The allowlist is path-based and narrower than `$HOME`: this process could write its assigned worktree, its assigned target root, and `/private/tmp`, but not the sibling dispatch worktree or `/Users/mingsun/.cargo-target/ai-dispatch/fix-grok-model-detection`. The exact original aid invocation's seatbelt profile was not directly inspectable; the observed allowlist is conclusive for this managed execution context, not a complete dump of the operator profile.
NEXT: NONE for the layer identification; do not attribute this to `src/sandbox.rs`, whose container wrapping is conditional on `spec.sandbox` at `src/background.rs:221-229`.

FINDING: The failing target is also unusable from the dispatched agent's Cargo context, while the agent and verifier do not have identical command environments.
CONFIDENCE: HIGH
EVIDENCE: The archived dispatched-agent environment printed `CARGO_TARGET_DIR=/Users/mingsun/.cargo-target/ai-dispatch/fix-grok-model-detection` and `HOME=/Users/mingsun/.aid/tasks/t-29be97a7/home` (`/Users/mingsun/.aid/tasks/t-29be97a7/transcript.md:79`). In that same agent run, `aid test` reported `CARGO_TARGET_DIR unwritable; fell back from /Users/mingsun/.cargo-target/ai-dispatch/fix-grok-model-detection to /var/folders/g2/415j1vrj7xz759r6drh380_00000gn/T/aid-build-target/grok-model-detection-e05e566fe1bea96a` (`/Users/mingsun/.aid/tasks/t-29be97a7/transcript.md:89`, `:92`, `:106`), while the transcript records writes and a commit under `/Users/mingsun/.aid/worktrees/ai-dispatch-6b31c929/fix/grok-model-detection` (`/Users/mingsun/.aid/tasks/t-29be97a7/transcript.md:33-41`, `:117`). Agent setup explicitly applies an isolated `HOME` and toolchain paths to the agent command (`src/agent/env.rs:206-227`; `src/background.rs:194-207`). Verify creates a fresh `Command::new(program)`, applies only the selected Cargo target environment, and sets cwd (`src/verify.rs:72-85`, `:203-235`); it does not copy the agent command's isolated `HOME`.
IMPLICATION: The agent can write its worktree, but Cargo cannot use the shared target in that agent context either; the agent's successful wrapper run is explained by fallback, not by target writability. Moving the same raw `cargo test` command into the agent would preserve the target-path denial and would not solve the EPERM. The agent-side transcript does not contain a raw byte probe performed by the agent itself, so a claim that the agent can write that target must not be made.
NEXT: NONE for the decision about injectable tools: environment relocation alone is not a fix; wrapper selection is the relevant difference.

FINDING: The verification path turns a Cargo environment failure into an agent-code failure because Cargo exits normally with a nonzero status after its child write is denied.
CONFIDENCE: HIGH
EVIDENCE: `run_verify_with_timeout` treats a spawned process with a non-success status as `VerifyResult.success = false` (`src/verify.rs:84-107`). `maybe_verify_impl` maps `Ok(result)` with `!result.success` to `record_verify_failed`, while only the `Err(e)` branch calls `record_verify_infrastructure_failure` (`src/cmd/run_verify.rs:50-83`). `record_verify_failed` persists `VerifyStatus::Failed` and calls `enforce_verify_status` (`src/cmd/run_verify.rs:109-118`); `enforce_verify_status` changes a `Done` task through `fail_completed_verify_gate` (`src/verify.rs:153-160`). The established error is a Cargo/rustc `.d` write error after `cargo` has started, so it follows the `Ok + success=false` branch. The lifecycle invokes this verifier before the final gate (`src/cmd/run_lifecycle.rs:369-383`).
IMPLICATION: The board outcome is indistinguishable from a broken delivery: `TaskOutcome::derive` maps `Done + VerifyStatus::Failed` to `Broken`, but maps `Done + InfrastructureFailure` to `Unverified(Infrastructure)` (`src/types/outcome.rs:110-125`). The existing taxonomy is therefore capable of an honest result, but this specific EPERM is not reaching it.
NEXT: A minimal honest-failure change would classify a target-path Cargo permission diagnostic as infrastructure after the attempt; a pre-run check requires a Cargo-specific probe, because a plain filesystem probe is not proven equivalent.

FINDING: `aid build` and `aid test` already contain the relevant target fallback and diagnostic behavior; the direct verify runner does not use it.
CONFIDENCE: HIGH
EVIDENCE: `build_fallback` matches Cargo's `Operation not permitted (os error 1)` / `Permission denied (os error 13)` only when the reported path is under the chosen target (`src/cmd/build_fallback.rs:7-14`, `:84-104`). `build_process` retries with a temp target and emits a note naming both target paths (`src/cmd/build_process.rs:109-153`). `aid test` uses that process and evaluates zero-match, no-target, harness, and Cargo failures into a named digest (`src/cmd/test_cmd.rs:18-44`, `:138-183`). Direct verify instead invokes the configured command through `src/verify.rs:203-235` and has no fallback branch.
IMPLICATION: The simplest durable repair is to share the existing Cargo runner for Rust verification, or to share its permission-diagnostic classifier and fallback. This changes the verifier behavior, not the outer sandbox allowlist.
NEXT: Rank option 1 below as the preferred implementation direction.

FINDING: The toolbox is prompt-injected Bash guidance, not an execution-context or environment-transfer mechanism.
CONFIDENCE: HIGH
EVIDENCE: A toolbox entry is metadata (`ToolMeta`) containing name, description, command, args, output format, tags, and scope (`src/toolbox.rs:28-37`). Resolution reads global/team/project definitions (`src/toolbox.rs:152-168`), and injection appends a text block telling the agent the tools are available via Bash (`src/toolbox.rs:184-223`). `aid tool test` runs the configured command with `std::process::Command::new(&tool.command).args(args).status()` in its caller's environment (`src/cmd/tool.rs:113-126`). The live `aid tool list` probe showed 25 global entries and no `aid-build`, `aid-test`, or `aid-verify` entry.
IMPLICATION: A definition such as `[tool] id = "aid-test"; command = "aid"; args = "test --bin aid"; tags = ["test", "verify"]` would make the wrapper discoverable, but the resulting `aid` child would still inherit the agent's environment and outer sandbox. It can benefit from the existing fallback; it cannot make raw verify run with the parent verifier's permissions, and it does not alter `run_verify`'s task classification.
NEXT: Treat toolbox entries as usability improvements after the lifecycle fix, not as the primary EPERM fix.

FINDING: Ranked fix options are: (1) route Rust verification through the existing Cargo runner; (2) classify target-path Cargo permission output as infrastructure; (3) add a Cargo-specific preflight; (4) inject wrapper commands as toolbox entries.
CONFIDENCE: MEDIUM
EVIDENCE: Option 1 has the existing implementation seams at `src/cmd/run_verify.rs:50-83`, `src/cmd/build_process.rs:109-153`, and `src/cmd/test_cmd.rs:18-44`. Option 2 can reuse the target/path matcher at `src/cmd/build_fallback.rs:10-14`, `:69-104`, then select `VerifyStatus::InfrastructureFailure` at `src/cmd/run_verify.rs:53-83`. Option 3 belongs before the status update/run at `src/cmd/run_verify.rs:44-52`. Option 4 uses the toolbox schema and injection path at `src/toolbox.rs:28-37`, `:214-224`.
IMPLICATION: (1) Cost: medium refactor, because verify currently has a synchronous `VerifyResult` API while the Cargo runner is async and store/event-aware. Solves the reproduced EPERM by retrying in a writable temp target and records the fallback; does not make the shared target writable or cover non-Cargo verifiers. (2) Cost: low. Preserves the raw command and returns a distinct unverified/infrastructure outcome; does not make verification pass and still waits for Cargo to fail. (3) Cost: low-to-medium. Can report before the real verification, but a plain byte-write probe is insufficient evidence because existing fallback code explicitly says sandbox blocks can be Cargo-path-specific (`src/cmd/build_fallback.rs:7-10`); an exact Cargo probe duplicates work. (4) Cost: low. Improves agent discoverability and lets agents choose `aid build`/`aid test`; does not change the verifier process, allowlist, or task outcome by itself.
NEXT: Implement option 1 plus option 2 if a distinct environment verdict is required; do not rely on option 4 alone.

SUMMARY: 6 findings, 5 high-confidence
