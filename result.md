## Findings

- Medium: Added hung-recovery policy and feedback helpers in [src/cmd/run_hung_recovery.rs](/tmp/aid-wt-feat/v868/hung-recovery/src/cmd/run_hung_recovery.rs#L12). Evidence: `MAX_HUNG_RETRIES` is capped at `2`, `should_auto_retry_hung()` gates retries on recorded progress, and `build_hung_retry_feedback()` includes the last progress detail plus the worktree continuation note.
- Medium: Both idle-timeout paths now record structured hung metadata before failing the task in [src/watcher.rs](/tmp/aid-wt-feat/v868/hung-recovery/src/watcher.rs#L67) and [src/pty_watch.rs](/tmp/aid-wt-feat/v868/hung-recovery/src/pty_watch.rs#L221). Evidence: each path calls `insert_hung_detected_events(...)`, which emits a `hung_detected` event and an `Agent hung: no output for N seconds` error with `hung_recovery_eligible`, `hung_duration_secs`, `event_count`, and `last_event_detail` metadata.
- Medium: Hung failures now auto-retry through `post_run_lifecycle()` in [src/cmd/run_lifecycle.rs](/tmp/aid-wt-feat/v868/hung-recovery/src/cmd/run_lifecycle.rs#L254). Evidence: `maybe_auto_retry_after_hang()` rebuilds the prompt with hung-specific feedback, decrements the existing retry budget, reuses the prior worktree target, and records `HUNG → retry` on the failed attempt for board/error display.
- Low: Verification passed for compilation but not for test execution. Evidence: `cargo check -p ai-dispatch` succeeded, while `cargo test -p ai-dispatch run_hung_recovery -- --nocapture` stopped on a pre-existing unrelated test compile error in [src/cmd/run_agent/tests.rs](/tmp/aid-wt-feat/v868/hung-recovery/src/cmd/run_agent/tests.rs#L168) because a `Task` fixture is missing `start_sha`.
