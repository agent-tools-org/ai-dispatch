// Explore helpers for choosing relevant files from a prompt.
// Exports a stub auto_detect_files hook for later implementation.

pub fn auto_detect_files(_prompt: &str, _root: &std::path::Path) -> Vec<String> {
    vec![]
}
